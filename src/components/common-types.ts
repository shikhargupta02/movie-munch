export type objectType = Record<string, any>;
export type rulesCheckedType = {
  hasNumber: boolean;
  hasUpperCase: boolean;
  hasLowerCase: boolean;
  hasSpecialChar: boolean;
  minLength: boolean;
};
